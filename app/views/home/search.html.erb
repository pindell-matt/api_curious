<h1>We’ve found <%= @results.total %> repository results</h1>

<div class="search-results">
  <% @results.search_items.each do |result| %>
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">
          <%= link_to result.name, result.html_url %>  <small><%= result.owner %></small>
        </h3>
      </div>
      <div class="panel-body">
        <%= result.description %>
      </div>
    </div>
  <% end %>
</div>
