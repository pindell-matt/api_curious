<h1>We’ve found <%= @results.total %> repository results</h1>

<% @results.search_items.each do |item| %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">
        <%= link_to item.name, item.html_url %>  <small><%= item.owner[:login] %></small>
      </h3>
    </div>
    <div class="panel-body">
      <%= item.description %>
    </div>
  </div>
<% end %>
